module.exports = {
  qtClient: {
    host: 'localhost',
    port: 15715,
    // localhost only, this shouldn't matter too much
    user: 'blackcoinrpc',
    // ditto as above
    pass: 'obsidian',
    timeout: 30000
  }
}
